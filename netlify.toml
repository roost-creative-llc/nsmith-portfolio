[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA routing is handled by public/_redirects (/* /index.html 200),
# which Vite copies into dist/ at build time.
